# Carrega a topologia declarativa versionada em docker/rabbitmq/definitions.json.
#
# Vai em conf.d (aditivo) para não sobrescrever o 10-defaults.conf da imagem oficial nem o
# 20-memory.conf deste repo. A importação roda no BOOT e é idempotente.
load_definitions = /etc/rabbitmq/definitions.json
